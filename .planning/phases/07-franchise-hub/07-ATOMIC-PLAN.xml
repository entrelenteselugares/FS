<phase name="07-franchise-hub">
  <!-- WAVE 1: DATA & INFRASTRUCTURE -->
  <step id="1_db_migration" name="DB Schema: Inventory & Referrals">
    <action>Add inventoryAlertThreshold (Int) to FranchiseProfile model.</action>
    <action>Add referralCode (String, Unique, Nullable) to User model.</action>
    <action>Add lastSupplyOrderAt (DateTime, Nullable) to FranchiseProfile.</action>
    <file>backend/prisma/schema.prisma</file>
  </step>

  <step id="2_referral_service" name="Backend: Referral System Logic">
    <action>Implement ReferralService to generate/validate unique codes.</action>
    <action>Update AuthController.register to accept 'ref' code and create ProfessionalNetwork link.</action>
    <file>backend/src/services/referral.service.ts</file>
    <file>backend/src/controllers/auth.controller.ts</file>
  </step>

  <!-- WAVE 2: MONITORING & ALERTS -->
  <step id="3_supply_logic" name="Backend: Supply Monitoring Engine">
    <action>Implement SupplyService to calculate remaining paper/credits using PhygitalPrint counts.</action>
    <action>Create cron-triggerable 'checkLowStock' function.</action>
    <file>backend/src/services/supply.service.ts</file>
  </step>

  <step id="4_supply_reorder" name="Backend: 1-Click Supply Order Engine">
    <action>Add adminCreateSupplyOrder endpoint to generate pre-filled Orders for franchises.</action>
    <file>backend/src/controllers/order.controller.ts</file>
  </step>

  <!-- WAVE 3: FRONTEND DASHBOARD -->
  <step id="5_franchise_ui_shell" name="UI: Franchise Dashboard Base">
    <action>Create src/pages/franchise/Dashboard.tsx using Midnight Luxury cards.</action>
    <action>Integrate 'Printer Agent' real-time connectivity status.</action>
    <file>frontend/src/pages/franchise/Dashboard.tsx</file>
  </step>

  <step id="6_inventory_widgets" name="UI: Inventory & Referral Widgets">
    <action>Build progress-based inventory trackers with 'Low Stock' visual alerts.</action>
    <action>Implement 'Copy Referral Link' component with toast feedback.</action>
    <file>frontend/src/components/franchise/InventoryStatus.tsx</file>
    <file>frontend/src/components/franchise/ReferralTool.tsx</file>
  </step>

  <!-- WAVE 4: VALIDATION -->
  <step id="7_uat_validation" name="UAT: End-to-End B2B Flow">
    <action>Test referral signup -> networking verification.</action>
    <action>Test inventory depletion -> stock alert trigger.</action>
    <action>Test 1-click reorder -> database entry validation.</action>
  </step>
</phase>
