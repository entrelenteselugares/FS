---
phase: 49-proximity-search-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    "backend/prisma/schema.prisma",
    "backend/src/controllers/marketplace.controller.ts",
    "frontend/src/pages/profissional/ConfigTab.tsx",
    "frontend/src/pages/ProfissionaisDirectory.tsx",
  ]
autonomous: true
requirements: ["SEARCH-01", "SEARCH-02", "SEARCH-03", "SEARCH-04"]

must_haves:
  truths:
    - "Profissional model has geolocation fields"
    - "MarketplaceController can filter by distance or city"
    - "Frontend Directory UI includes a proximity filter"
  artifacts:
    - "backend/prisma/schema.prisma"
  key_links: []
---

<objective>
Enhance the professional marketplace directory with proximity search and geographic filtering capabilities.

Purpose: Enable clients to find nearby professionals easily, increasing the relevance of search results and prioritizing verified/PRO members.
Output: Schema updates for geo-coordinates, backend search logic using Haversine formula (or simple city matching), and frontend map/distance UI.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

MarketplaceController currently returns all professionals with an active PRO subscription.
We need to add geolocation fields to `Profissional` to allow radius filtering.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Geolocation Fields to Profissional Schema</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
    Add the following fields to the `Profissional` model:
    - `baseLocationLat` Float?
    - `baseLocationLng` Float?
    - `serviceRadiusKm` Int @default(50)
    - `city` String?
  </action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - Geolocation fields exist on `Profissional`.
  </acceptance_criteria>
  <done>Schema valid</done>
</task>

<task type="auto">
  <name>Task 2: Sync Schema and Generate Client</name>
  <files>backend/prisma/schema.prisma</files>
  <action>Run `npx prisma db push --accept-data-loss` and `npx prisma generate`.</action>
  <verify>Database is synced.</verify>
  <acceptance_criteria>
    - Database is updated with new fields.
  </acceptance_criteria>
  <done>Schema synced</done>
</task>

<task type="auto">
  <name>Task 3: Backend Search Logic Update</name>
  <files>backend/src/controllers/marketplace.controller.ts</files>
  <action>
    Update `MarketplaceController.listProfissionais`:
    - Accept `lat`, `lng`, `radius`, `city` from `req.query`.
    - If `city` is provided, filter by `Profissional.city` (case-insensitive).
    - If `lat` and `lng` are provided, calculate distance using a basic Haversine formula in JavaScript after fetching candidates (or use Prisma filtering if possible, but JS filtering is fine for MVP), filtering those within `serviceRadiusKm`.
    - Sort results: Verified PROs first, then by `agilityPoints`, then by distance (if lat/lng provided).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - API accepts geographic params and filters correctly.
  </acceptance_criteria>
  <done>Backend logic updated</done>
</task>

<task type="auto">
  <name>Task 4: Update Professional Settings for Geolocation</name>
  <files>frontend/src/pages/profissional/ConfigTab.tsx</files>
  <action>
    Add inputs for `city` and `serviceRadiusKm` to the professional configuration/settings page (or wherever professional profile updates happen, like `updateProfile` in `profissional.controller.ts` which modifies `Profissional`).
    If `frontend/src/pages/profissional/ConfigTab.tsx` doesn't exist or isn't the right place, look for the profile editing component (e.g. `ProfileSettings.tsx` or similar in `frontend/src/pages/profissional/`).
    Update the backend `profissional.controller.ts` `updateProfile` method to save these new fields.
  </action>
  <verify>npm run build in frontend</verify>
  <acceptance_criteria>
    - Professional can save city and radius.
  </acceptance_criteria>
  <done>Settings UI updated</done>
</task>

<task type="auto">
  <name>Task 5: Add Proximity Filters to Directory UI</name>
  <files>frontend/src/pages/ProfissionaisDirectory.tsx</files>
  <action>
    In the public directory (usually mapped to `ProfissionaisDirectory.tsx` or `Showcase.tsx`), add UI elements to:
    - Filter by City (text input).
    - Toggle "Near Me" (which requests HTML5 geolocation and passes `lat`/`lng` to the API).
    Update the API call to pass these new parameters.
  </action>
  <verify>npm run build in frontend</verify>
  <acceptance_criteria>
    - User can filter directory geographically.
  </acceptance_criteria>
  <done>Directory UI updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend compiles cleanly (`npx tsc --noEmit`)
- [ ] Frontend builds successfully (`npm run build`)
</verification>

<success_criteria>

- All tasks completed
- Schema updated
- Directory filtering works
  </success_criteria>

<output>
After completion, create `.planning/phases/49-proximity-search-directory/49-01-SUMMARY.md`
</output>
