---
phase: 48-automated-booking-escrow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/prisma/schema.prisma", "backend/src/controllers/payment.controller.ts", "backend/src/jobs/vault-cycle.job.ts"]
autonomous: true
requirements: ["ESCROW-01", "ESCROW-02", "ESCROW-03", "ESCROW-04"]

must_haves:
  truths:
    - "ServiceBooking model exists in schema"
    - "Webhook handles booking fee payments and updates status"
    - "Escrow release logic exists"
  artifacts:
    - "backend/prisma/schema.prisma"
  key_links: []
---

<objective>
Implement the Automated Booking Escrow system to manage reservation fees.

Purpose: Guarantee professional commitment and monetize lead flow by holding a reservation fee until the service is complete or a timeout expires.
Output: ServiceBooking schema, Webhook handler for bookings, and Escrow release job.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

MarketplaceController already attempts to create `ServiceBooking` and initiate MercadoPago, but the model is missing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ServiceBooking Model to Schema</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first></read_first>
  <action>
    Add the `ServiceBooking` model to `schema.prisma`. It must have:
    - `id` (String, @id, @default(cuid()))
    - `clienteName` (String)
    - `clienteEmail` (String)
    - `clientePhone` (String)
    - `profissionalId` (String)
    - `packageDesc` (String)
    - `bookingFee` (Decimal)
    - `status` (String, default: "PENDING") // PENDING, PAID, RELEASED, CANCELLED
    - `paymentId` (String?)
    - `createdAt` (DateTime)
    - `updatedAt` (DateTime)
    
    Add a relation to `Profissional` via `profissionalId`.
  </action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - `ServiceBooking` model exists.
  </acceptance_criteria>
  <done>Schema valid</done>
</task>

<task type="auto">
  <name>Task 2: Sync Schema and Generate Prisma Client</name>
  <files>backend/prisma/schema.prisma</files>
  <action>Run `npx prisma db push --accept-data-loss` to sync the database, then `npx prisma generate`.</action>
  <verify>Database is synced.</verify>
  <acceptance_criteria>
    - Command succeeds.
  </acceptance_criteria>
  <done>Schema pushed to DB.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Webhook Handling for Booking Payments</name>
  <files>backend/src/controllers/payment.controller.ts</files>
  <read_first>backend/src/controllers/mercadopago.controller.ts</read_first>
  <action>
    Update `PaymentController.mercadopagoWebhook` to intercept `orderId` starting with `booking-`.
    In the webhook, extract the real ID (e.g., `const [prefix, realBookingId] = external_reference.split("-")`).
    If it's a booking, update the `ServiceBooking` status to "PAID" and store the `paymentId`.
    Also, create a `Payout` record for the professional with `status="PENDING"` and `amount = bookingFee` representing the escrowed value, storing the bookingId in a metadata field or note so it can be released later.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Webhook identifies `booking-` prefix.
    - Updates ServiceBooking status.
  </acceptance_criteria>
  <done>Webhook handles booking payments.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Escrow Release Cron Job</name>
  <files>backend/src/jobs/escrow-release.job.ts, backend/src/controllers/cron.controller.ts</files>
  <action>
    Create a job `backend/src/jobs/escrow-release.job.ts`.
    It should find all `ServiceBooking` where `status = "PAID"` and `createdAt` is older than 7 days.
    For each, update the status to "RELEASED", and find the corresponding `Payout` (with `status="PENDING"`) and update it to `status="APPROVED"` (or whichever status means the money is available to the professional).
    Export the job function `runEscrowReleaseJob`.
    Register it in `backend/src/controllers/cron.controller.ts` (if applicable) or expose an endpoint `POST /api/cron/escrow-release` in `backend/src/routes/index.ts` so it can be triggered by external cron.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Job correctly identifies 7-day old paid bookings.
    - Updates booking and payout status.
  </acceptance_criteria>
  <done>Escrow release logic implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run build` in the backend to ensure it compiles without errors.
</verification>

<success_criteria>
- All tasks completed
- Webhook handles ServiceBooking
- Escrow releases after 7 days
</success_criteria>

<output>
After completion, create `.planning/phases/48-automated-booking-escrow/48-01-SUMMARY.md`
</output>
