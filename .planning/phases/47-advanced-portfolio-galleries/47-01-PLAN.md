---
phase: 47-advanced-portfolio-galleries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/prisma/schema.prisma, backend/src/controllers/PortfolioController.ts, backend/src/routes/portfolioRoutes.ts, frontend/src/pages/profissional/PortfolioManage.tsx, frontend/src/pages/PublicProfile.tsx]
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04]

must_haves:
  truths:
    - "User can upload and organize portfolio images into albums"
    - "Public profiles show the portfolio in a masonry grid"
  artifacts:
    - path: "backend/src/controllers/PortfolioController.ts"
      provides: "CRUD for portfolio albums and images"
    - path: "frontend/src/pages/profissional/PortfolioManage.tsx"
      provides: "UI for managing portfolio"
  key_links:
    - from: "frontend/src/pages/profissional/PortfolioManage.tsx"
      to: "/api/portfolio"
      via: "fetch"
---

<objective>
Implement the Advanced Portfolio Galleries feature for professionals.

Purpose: Allow professionals to upload, categorize, and showcase high-quality image albums on their public profiles.
Output: Prisma schema updates for PortfolioAlbum and PortfolioImage, backend REST API, and frontend management and public viewing components.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files (backend/prisma/schema.prisma). The planner MUST include a `[BLOCKING]` task that runs the database schema push command AFTER all schema file modifications are complete but BEFORE verification.

- ORM detected: Prisma
- Push command: npx prisma db push --accept-data-loss
- Non-TTY workaround: npx prisma db push --accept-data-loss
</schema_push_requirement>

<tasks>
<task type="auto">
  <name>Task 1: Update Prisma Schema</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>Add two new models: `PortfolioAlbum` (id, profissionalId, title, description, coverUrl, createdAt) and `PortfolioImage` (id, albumId, url, thumbnailUrl, watermarkedUrl, aiTags, createdAt). Add relations to `Profissional` model (albums PortfolioAlbum[]). Then run `npx prisma format`.</action>
  <verify>npx prisma validate</verify>
  <done>Schema compiles successfully</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Database Schema Push</name>
  <action>Run `cd backend && npx prisma db push --accept-data-loss` to update the database schema.</action>
  <verify>Schema push completes</verify>
  <done>Database is updated</done>
</task>

<task type="auto">
  <name>Task 3: Backend Portfolio API</name>
  <files>backend/src/controllers/PortfolioController.ts, backend/src/routes/portfolioRoutes.ts, backend/src/server.ts</files>
  <read_first>backend/src/routes/index.ts</read_first>
  <action>Create a `PortfolioController` with methods: `getAlbums`, `getAlbumImages`, `createAlbum`, `uploadImages`. In `uploadImages`, stub the logic for generating thumbnails/watermarks. Register routes in `portfolioRoutes.ts` and attach to `server.ts` or `index.ts` under `/api/portfolio`.</action>
  <verify>tsc --noEmit</verify>
  <done>API endpoints are defined and integrated</done>
</task>

<task type="auto">
  <name>Task 4: Frontend Management UI</name>
  <files>frontend/src/pages/profissional/PortfolioManage.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/pages/profissional/DashboardHeader.tsx</read_first>
  <action>Create the `PortfolioManage` component in `frontend/src/pages/profissional/`. It should have a clean, modern UI to create albums and upload photos. Use the `Editorial Premium` design system (theme-surface, theme-border, brand-tactical). Register route in `App.tsx`.</action>
  <verify>tsc --noEmit</verify>
  <done>Component compiles without TS errors</done>
</task>

<task type="auto">
  <name>Task 5: Public Profile Masonry Gallery</name>
  <files>frontend/src/pages/PublicProfile.tsx</files>
  <read_first>frontend/src/pages/PublicProfile.tsx</read_first>
  <action>Update `PublicProfile.tsx` to fetch the professional's albums and render them in a masonry grid. The grid should use the new design system styles. Display album cover images.</action>
  <verify>tsc --noEmit</verify>
  <done>PublicProfile updated</done>
</task>
</tasks>

<verification>
- [ ] npm run build passes in both backend and frontend
- [ ] Database schema is updated
</verification>

<success_criteria>
- All tasks completed successfully
- No TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-advanced-portfolio-galleries/47-01-SUMMARY.md`
</output>
