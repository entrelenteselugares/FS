const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const event = await prisma.event.findUnique({ where: { slug: 'fp-na-casa-do-baiano--iutu' } }); console.log('dataEvento:', event.dataEvento); console.log('eventHours:', event.eventHours); console.log('eventDays:', event.eventDays); } main().finally(() => prisma.$disconnect());
